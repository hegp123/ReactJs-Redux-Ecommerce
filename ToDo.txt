## instalar Redux
$ npm install --save redux

## copia las llaves del estado actual  '...'
...state


## instalar la libreria react redux
$ npm install --save react-redux

## Nos permite definir un archivo json que funciona como respuesta de un consumo rest 
## osea simula que estuvieramos consumiendo un endpoint (backend) y nos devuelve un json, que es el archivo que definimos
## ojo debe ser -g :  global, sino no funcoina
## para ejecutarlos json-server -p3001 products.json
## este seria el endpoint:  http://localhost:3001/products
npm install -g json-server   

## instalar la middleware para llamados asincronos
$ npm install --save redux-thunk

## instalar la libreria para consumir servicios 
$ npm install --save axios